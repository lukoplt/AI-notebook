<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AINotebook.App.Views.GlobalSearchDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AINotebook.App.ViewModels"
    x:Name="Dialog"
    CloseButtonText="Close"
    DefaultButton="None">
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMinWidth">640</x:Double>
        <x:Double x:Key="ContentDialogMaxWidth">800</x:Double>
    </ContentDialog.Resources>
    <Grid MinHeight="400" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search box with keyboard accelerator -->
        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="QueryBox"
                     Text="{x:Bind ViewModel.Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PlaceholderText="{x:Bind SearchPlaceholder}"
                     KeyDown="OnQueryKeyDown"/>
            <Button Grid.Column="1" Command="{x:Bind ViewModel.SearchCommand}"
                    Style="{ThemeResource AccentButtonStyle}"
                    x:Name="SearchButton"/>
        </Grid>

        <ProgressBar Grid.Row="1" IsIndeterminate="True"
                     Visibility="{x:Bind ViewModel.IsSearching, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"/>

        <ScrollViewer Grid.Row="2">
            <StackPanel Spacing="8">
                <!-- Notes section -->
                <TextBlock x:Name="NotesSectionLabel" Style="{ThemeResource BodyStrongTextBlockStyle}"
                           Visibility="{x:Bind ViewModel.Notes.Count, Mode=OneWay, Converter={StaticResource CountToVisibility}}"/>
                <ItemsControl ItemsSource="{x:Bind ViewModel.Notes, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:NoteSearchHit">
                            <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                    Click="OnNoteHitClick" Tag="{x:Bind}"
                                    Padding="10,8" Margin="0,2">
                                <StackPanel Spacing="2">
                                    <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind Snippet}" TextWrapping="Wrap" FontSize="12"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Sources section -->
                <TextBlock x:Name="SourcesSectionLabel" Style="{ThemeResource BodyStrongTextBlockStyle}"
                           Margin="0,8,0,0"
                           Visibility="{x:Bind ViewModel.Sources.Count, Mode=OneWay, Converter={StaticResource CountToVisibility}}"/>
                <ItemsControl ItemsSource="{x:Bind ViewModel.Sources, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:SourceSearchHit">
                            <Border Padding="10,8" Margin="0,2" CornerRadius="4"
                                    Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty hint -->
                <TextBlock x:Name="EmptyHint" TextAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
