<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AINotebook.App.Dialogs.NoteHistoryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AINotebook.App.ViewModels"
    xmlns:models="using:AINotebook.Core.Models"
    DefaultButton="Close">
    <Grid Width="720" Height="460">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Versions list (newest-first) -->
        <ListView Grid.Column="0"
                  ItemsSource="{x:Bind ViewModel.Versions, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:NoteVersion">
                    <StackPanel Spacing="2" Padding="0,4">
                        <TextBlock Text="{x:Bind Reason}" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="{x:Bind SavedAt}" Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Preview of the selected version -->
        <Grid Grid.Column="1" Padding="16,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Style="{ThemeResource SubtitleTextBlockStyle}"
                       Text="{x:Bind ViewModel.Selected.Title, Mode=OneWay}"
                       TextWrapping="Wrap"/>

            <ScrollViewer Grid.Row="1" Margin="0,8,0,8">
                <TextBlock Text="{x:Bind ViewModel.Selected.BodyMd, Mode=OneWay}"
                           FontFamily="Consolas" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
            </ScrollViewer>

            <Button Grid.Row="2" x:Name="RestoreButton" HorizontalAlignment="Right"
                    Style="{ThemeResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.RestoreCommand}"/>
        </Grid>
    </Grid>
</ContentDialog>
